$(document).ready(function() {
	document.getElementById("loginForm").addEventListener("submit", loginFormSubmit, false);
});

function loginFormSubmit(e) {
	e.preventDefault();
	//TODO make ajax request verification
}

function navigateToHomePage() {
	//On ajax success
}